import { mockUser } from "../context/mockData/mockUser";
import { mockFollowers } from "../context/mockData/mockFollowers";
import { mockRepos } from "../context/mockData/mockRepos";

export const data = {
    User: mockUser,
    Followers: mockFollowers,
    Repos: mockRepos
};